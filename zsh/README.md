To use this .zshrc please also run this to create the necessary folder structure:
```
mkdir -p ~/.cache/zsh
```

Also run the following:
```
mkdir -p /opt/zsh && cd /opt/zsh && git clone git://github.com/zsh-users/zsh-syntax-highlighting.git
```
